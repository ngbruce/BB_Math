# 实现任务清单

## 1. 修复 Random 实例重复初始化问题
- [x] 1.1 在 `UI/MainFormPresenter.cs` 中添加 `_mathProblemGenerator` 私有成员变量
- [x] 1.2 修改 `MainFormPresenter` 构造函数，初始化 `_mathProblemGenerator = new MathProblemGenerator(_random)`
- [x] 1.3 修改 `GenerateProblem()` 方法，将 `var generator = new MathProblemGenerator()` 改为重用 `_mathProblemGenerator`
- [x] 1.4 验证所有策略的随机数生成都使用同一个 Random 实例（通过 MathProblemGenerator 传入 _random 实现）

## 2. 验证所有难度级别的出题逻辑
### 2.1 验证 LV1 难度（个位数加减法）
- [x] 2.1.1 运行 15-30 道题目，验证操作数范围 1-9
- [x] 2.1.2 验证加法结果 2-18，减法结果>=0
- [x] 2.1.3 确认无重试失败错误

### 2.2 验证 LV2 难度（100以内加减法）
- [x] 2.2.1 运行 15-30 道题目，验证操作数范围 1-99
- [x] 2.2.2 验证加法结果<100
- [x] 2.2.3 验证减法结果>=0
- [x] 2.2.4 确认无重试失败错误（这是触发本次修复的问题）

### 2.3 验证 LV3 难度（个位数乘除法）
- [x] 2.3.1 运行 15-30 道题目，验证操作数范围 1-9
- [x] 2.3.2 验证乘法结果 1-81
- [x] 2.3.3 验证除法能整除，无余数
- [x] 2.3.4 确认无重试失败错误

### 2.4 验证 LV4 难度（100以内加减乘除带余数）
- [x] 2.4.1 运行 15-30 道题目，验证操作数范围 1-99
- [x] 2.4.2 验证加减法结果<100
- [x] 2.4.3 验证乘法使用个位数，结果<100（effectiveIntBits=1）
- [x] 2.4.4 验证除法使用个位数（1-9）
- [x] 2.4.5 确认无重试失败错误

### 2.5 验证 LV5 难度（100以上加减乘除带余数）
- [x] 2.5.1 运行 15-30 道题目，验证操作数范围 1-999
- [x] 2.5.2 验证加法结果≤2000
- [x] 2.5.3 验证减法结果0-899
- [x] 2.5.4 验证乘法使用两位数（10-99），结果100-9801
- [x] 2.5.5 验证除法使用两位数（10-99）
- [x] 2.5.6 确认无重试失败错误

## 3. 检查日志输出和随机性
- [x] 3.1 运行测试，检查日志中是否还有重复的"重试题目"日志（相同的题目重复出现）
- [x] 3.2 验证每次重试都生成不同的题目（通过日志观察操作数变化）
- [x] 3.3 确认重试次数显著减少（大多数题目一次或少数几次就满足条件）

## 4. 压力测试
- [x] 4.1 在 LV1 难度下运行 100 道题，验证稳定性和随机性
- [x] 4.2 在 LV2 难度下运行 100 道题，验证稳定性和随机性（重点测试）
- [x] 4.3 在 LV3 难度下运行 100 道题，验证稳定性和随机性
- [x] 4.4 在 LV4 难度下运行 100 道题，验证稳定性和随机性
- [x] 4.5 在 LV5 难度下运行 100 道题，验证稳定性和随机性

## 5. 更新规范文档
- [x] 5.1 修改 `openspec/changes/fix-all-difficulty-random-generation-bug/specs/math-operations/spec.md`
- [x] 5.2 添加关于随机数生成器生命周期的新要求
- [x] 5.3 确保规范明确要求 Random 实例应重用，而非每次创建新实例

## 6. 验证和文档更新
- [x] 6.1 运行 `openspec validate fix-all-difficulty-random-generation-bug --strict` 验证变更
- [x] 6.2 更新 CHANGELOG.md（如有需要）
- [x] 6.3 确认所有测试通过，问题已彻底解决
