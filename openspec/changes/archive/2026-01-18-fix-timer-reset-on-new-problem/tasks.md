# 实现任务清单

## 1. 扩展 IMainFormView 接口
- [x] 1.1 在 `UI/MainFormPresenter.cs` 的 `IMainFormView` 接口中添加 `ResetTimer()` 方法声明
- [x] 1.2 添加方法注释，说明其用途

## 2. 实现 ResetTimer() 方法
- [x] 2.1 在 `Form1.cs` 中实现 `IMainFormView.ResetTimer()` 方法
- [x] 2.2 实现逻辑：重置 `counterTimeOut` 为当前题型的 `TimeLimit`
- [x] 2.3 实现逻辑：重启定时器 `timer1.Start()`
- [x] 2.4 添加日志输出，便于调试

## 3. 在生成新题时调用定时器重置
- [x] 3.1 在 `UI/MainFormPresenter.cs` 的 `GenerateProblem()` 方法中，调用 `_view.DisplayProblem()` 后添加 `_view.ResetTimer()`
- [x] 3.2 确认位置正确（第316行之后）

## 4. 更新规范文档
- [x] 4.1 在 `openspec/changes/fix-timer-reset-on-new-problem/specs/time-management/spec.md` 中添加定时器重置要求
- [x] 4.2 添加场景描述：每题生成后应重置定时器
- [x] 4.3 添加场景描述：超时惩罚后应重置定时器

## 5. 验证功能
### 5.1 验证所有难度级别
- [x] 5.1.1 测试 LV1 难度，完成一题后，验证倒计时重置为60秒
- [x] 5.1.2 测试 LV2 难度，完成一题后，验证倒计时重置为60秒
- [x] 5.1.3 测试 LV3 难度，完成一题后，验证倒计时重置为60秒
- [x] 5.1.4 测试 LV4 难度，完成一题后，验证倒计时重置为60秒
- [x] 5.1.5 测试 LV5 难度，完成一题后，验证倒计时重置为60秒

### 5.2 验证所有题型
- [x] 5.2.1 测试加法题型，验证倒计时重置
- [x] 5.2.2 测试减法题型，验证倒计时重置
- [x] 5.2.3 测试乘法题型，验证倒计时重置
- [x] 5.2.4 测试无余数除法题型，验证倒计时重置
- [x] 5.2.5 测试有余数除法题型，验证倒计时重置

### 5.3 验证超时惩罚机制
- [x] 5.3.1 测试答题超时，验证惩罚机制正常工作（增加题目数量）
- [x] 5.3.2 超时后重新开始答题，验证倒计时正确重置
- [x] 5.3.3 连续超时多次，验证每次都正确重置

### 5.4 压力测试
- [x] 5.4.1 连续答题10题，验证每题倒计时都正确重置
- [x] 5.4.2 观察日志，确认定时器重置调用时机正确
- [x] 5.4.3 检查没有重复启动定时器的问题

## 6. 验证和文档更新
- [x] 6.1 运行 `openspec validate fix-timer-reset-on-new-problem --strict` 验证变更
- [x] 6.2 确认所有测试通过，问题已彻底解决
