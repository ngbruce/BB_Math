---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-01-12T09:17:02.335Z
provider: 
---

# C# 开发环境约束规则

## 环境分工约束

### VS Code 职责范围
- **允许**：代码编写、文档维护、文件读写、文本搜索、规范管理
- **禁止**：编译、调试、运行 C# 工程

### Visual Studio 2019 职责范围
- **必须**：所有编译、调试、运行操作在此完成
- **必须**：窗体设计、控件添加/修改在此完成

## AI 助手禁止操作

### 严格禁止的操作
- ❌ **禁止** 修改或创建 `*.Designer.cs` 文件
- ❌ **禁止** 修改或创建窗体设计器生成的代码
- ❌ **禁止** 直接添加/删除控件或修改控件属性
- ❌ **禁止** 自动添加控件事件处理函数

### 窗体/控件修改流程
当需要修改窗体或控件时，AI 助手必须：

1. **暂停编程**，输出明确的操作指令
2. **提供详细说明**：
   - 需要添加的控件类型和名称
   - 需要设置的属性
   - 需要添加的事件处理函数签名
   - 控件在界面上的位置和布局建议
3. **要求用户手动操作**：在 VS IDE 中使用窗体设计器完成
4. **等待用户确认**：用户完成操作后明确告知 AI
5. **继续编程**：收到确认后继续后续代码编写

示例对话流程：
```
AI: 需要在 Form1 上添加一个 Label 控件，请按以下步骤操作：
   1. 在 VS 设计器中打开 Form1.cs
   2. 从工具箱拖拽 Label 控件到窗体
   3. 设置 Name = "lbNewLabel"
   4. 设置 Text = "新标签"
   5. 设置 Location = (10, 10)
   6. 完成后请告诉我

用户: 已完成添加

AI: 好的，现在我可以继续编写引用该控件的代码...
```

## 编译与调试约束

### AI 助手必须提醒用户
当 AI 助手生成或修改代码后：
- ⚠️ 必须明确提醒用户："请回到 VS 2019 中编译项目"
- ⚠️ 要求用户反馈："请报告是否有编译错误"

### 错误处理流程
1. **用户报告编译错误后**：
   - AI 分析错误信息
   - 查看相关代码文件
   - 提供修复建议或直接修改代码

2. **用户报告运行时异常后**：
   - AI 分析异常信息
   - 查看相关代码逻辑
   - 提供调试建议或代码修正

### 反馈要求
AI 助手必须明确要求用户反馈以下信息：
- 编译是否成功（是/否）
- 如有错误，完整的错误消息和错误行号
- 运行时异常的类型和堆栈信息
- 程序行为是否符合预期

## 代码修改范围

### 允许修改的文件类型
- ✅ `*.cs` （不包括 `*.Designer.cs`）
- ✅ `*.md` 文档文件
- ✅ `*.csproj` 项目文件
- ✅ `*.config` 配置文件
- ✅ `openspec/` 目录下的所有规范文件

### 禁止修改的文件类型
- ❌ `*.Designer.cs` - 窗体设计器文件
- ❌ `*.resx` - 资源文件（除非有特殊说明）
- ❌ 自动生成的文件（如 `Resources.Designer.cs`）

## 规范文件优先原则

在实施任何代码变更前，AI 助手必须：
1. 检查是否有相关的 OpenSpec 变更提案（`openspec/changes/`）
2. 确保变更符合已批准的规范
3. 对于未规范的变更，提醒用户创建变更提案

## 与本规则冲突的处理

如果用户的直接指令与本规则冲突：
1. AI 应礼貌地指出冲突点
2. 解释为什么不能执行该操作
3. 提供符合规范的替代方案
4. 如需例外，要求用户明确确认已理解风险